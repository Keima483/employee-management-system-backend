spring:
  datasource:
    url: ${env.MYSQL_URL}
    username: ${env.MYSQL_USERNAME}
    password: ${env.MYSQL_PASSWORD}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true